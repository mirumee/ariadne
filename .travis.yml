dist: xenial
language: python
python:
  - 3.6
  - 3.7
install:
  - pip install -e .
  - pip install -r requirements-dev.txt
script:
  - pytest --cov
after_success:
  - codecov
matrix:
  include:
    - python: 3.7
      script:
        - pylint ariadne tests setup.py
        - mypy ariadne --ignore-missing-imports
        - black --check .